<?xml version="1.0" encoding="utf-8"?>
<odoo>

        <record id="res_config_settings_view_form_cheque" model="ir.ui.view">
            <field name="name">res.config.settings.view.form.inherit.ff_cheque_management</field>
            <field name="model">res.config.settings</field>
            <field name="inherit_id" ref="account.res_config_settings_view_form"/>
            <field name="arch" type="xml">
                <xpath expr="//block[@name='fiscal_localization_setting_container']" position="after">
                    <block title="Cheque Management" name="cheque_management_setting_container">
                        <setting id="incoming_cheque_account_setting" company_dependent="1"
                                 string="Incoming Cheques Account"
                                 help="Account where cheques received from customers are parked until they are collected through the bank reconciliation (e.g. Cheques Under Collection). Must be reconcilable.">
                            <field name="incoming_cheque_account_id"
                                   options="{'no_create': True}"
                                   placeholder="e.g. Cheques Under Collection"/>
                        </setting>
                        <setting id="outgoing_cheque_account_setting" company_dependent="1"
                                 string="Outgoing Cheques Account"
                                 help="Account where cheques issued to vendors are parked until they are cashed through the bank reconciliation (e.g. Issued Cheques). Must be reconcilable.">
                            <field name="outgoing_cheque_account_id"
                                   options="{'no_create': True}"
                                   placeholder="e.g. Issued Cheques"/>
                        </setting>
                    </block>
                </xpath>
            </field>
        </record>

</odoo>
